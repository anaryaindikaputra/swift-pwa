import { IconButton } from '@root/node_modules/@material-ui/core';
import { AddShoppingCart } from '@root/node_modules/@material-ui/icons/index';

const AddToCartButton = () => (
    <IconButton aria-label="add to shopping cart"><AddShoppingCart /></IconButton>
);

export default AddToCartButton;
